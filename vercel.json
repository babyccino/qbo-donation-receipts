{
  "installCommand": "bun install && bun run patch",
  "buildCommand": "npx next build",
  "headers": [
    {
      "source": "/:path*",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache"
        }
      ]
    }
  ]
}
